<Window x:Class="Eclipse_Library.UI.SkillSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Additional Skill"
        Height="620"
        Width="780"
        MinHeight="500"
        MinWidth="700"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,4,8,8" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="12,4" />
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="#F2E6CB" Padding="8" Margin="0,0,0,8">
            <TextBlock Text="Add Additional Skill" FontWeight="SemiBold" TextAlignment="Center" />
        </Border>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBox x:Name="SearchTextBox" Width="240" MinHeight="28" TextChanged="SearchTextBox_TextChanged" />
            <ComboBox x:Name="CategoryFilterComboBox" Width="180" Margin="8,0,0,0" SelectionChanged="CategoryFilterComboBox_SelectionChanged" />
            <TextBlock x:Name="CountText" DockPanel.Dock="Right" Margin="12,0,0,0" Foreground="#555" />
        </DockPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListBox x:Name="SkillsListBox"
                     DisplayMemberPath="DisplayName"
                     SelectionChanged="SkillsListBox_SelectionChanged" />

            <RichTextBox Grid.Column="1"
                         x:Name="SkillDescriptionRichTextBox"
                         Margin="12,0,0,0"
                         FontFamily="Consolas"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True" />
        </Grid>

        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add &amp; Close" Width="110" Click="AddClose_Click" />
                <Button Content="Add" Width="86" Click="Add_Click" />
                <Button Content="Close" Width="86" Click="Close_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
